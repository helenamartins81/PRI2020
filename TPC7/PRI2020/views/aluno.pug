
extends layout
block content
    .w3-container
        .w3-card-4
            header.w3-container.w3-teal.w3-margin-top
                h3 Dados do Aluno
        .w3-card-4
            address.w3-right
                
                form(method='GET' action=`/alunos/editar/${aluno._id}`)
                    input(type='submit' value='Editar')

                form(method='POST' action=`/eliminar/${aluno._id}`)
                    input(type='submit' value='Eliminar')

            table.w3-table-all
                tr
                    th Número:
                        td= aluno.Número
                tr
                    th Nome:
                        td= aluno.Nome
                tr
                    th Git:
                        td= aluno.Git
                
                tr 
                    th TPC: 
                        td
                            ul
                                each val, index in aluno.tpc
                                    if(val==1)
                                        li TPC #{index}: Feito
                                    else
                                        li TPC #{index}: Não feito`

                                
        
            address.w3-right Voltar à página principal 
                a(href="/alunos/") Voltar
            footer(class="w3-container w3-teal")
                address Gerado por galuno :: 2020-Novembro, helena